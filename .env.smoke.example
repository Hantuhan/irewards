# Local-only smoke credentials (gitignored). Copy to `.env.smoke` and fill in.
# Never commit real passwords. Never put these in the login UI or public docs.

SMOKE_OWNER_EMAIL=
SMOKE_OWNER_PASSWORD=
SMOKE_STAFF_EMAIL=
SMOKE_STAFF_PASSWORD=
PLATFORM_ADMIN_PASSWORD=
