/** Storefront hero banner — fixed aspect ratio (mobile-first, similar to Facebook cover). */
export const STOREFRONT_BANNER_ASPECT = 3.2;
export const STOREFRONT_BANNER_OUTPUT_WIDTH = 960;
export const STOREFRONT_BANNER_OUTPUT_HEIGHT = Math.round(
  STOREFRONT_BANNER_OUTPUT_WIDTH / STOREFRONT_BANNER_ASPECT,
);
